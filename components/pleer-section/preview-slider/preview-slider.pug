mixin preview-slider({data, title, text, imgSrc})
    +b.preview-slider
        +e.img-wrapper
            +e.img(src=imgSrc)= imgSrc
        +e.P.data= data
        +e.H1.title= title
        +e.P.text= text

